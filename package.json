{
  "name": "agenda-clo",
  "version": "0.1.0",
  "description": "Per-project task/todo lists for OpenClaw (project = agent).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node store.ts && node smoke.mjs",
    "smoke": "node smoke.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.48"
  },
  "devDependencies": {
    "@types/node": "^22",
    "jiti": "^2.7.0"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
