{
  "id": "agenda-clo",
  "name": "AgendaClo",
  "description": "Per-project task/todo lists for OpenClaw (project = agent).",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "storePath": {
        "type": "string",
        "description": "Where tasks.json lives. Defaults to the plugin state dir."
      }
    },
    "additionalProperties": false
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "task"
    ]
  }
}
