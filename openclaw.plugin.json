{
  "id": "agenda-clo",
  "name": "AgendaClo",
  "description": "Per-user project records (status + notes) for OpenClaw, with a Telegram Mini App.",
  "version": "0.4.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "storePath": {
        "type": "string",
        "description": "Where tasks.json lives. Defaults to the plugin state dir."
      },
      "ownerIds": {
        "type": "array",
        "items": { "type": "number" },
        "description": "Telegram user ids allowed to use the Mini App. Empty = any valid Telegram user of this bot."
      }
    },
    "additionalProperties": false
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": ["project"]
  }
}
